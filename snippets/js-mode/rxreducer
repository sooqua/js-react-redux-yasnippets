# -*- mode: snippet -*-
# name: rxreducer
# key: rxreducer
# --
const initialState = {

}`(es7-snippets-semicolon)`

export default (state = initialState, { type, payload }) => {
    switch (type) {

    case ${typeName}:
        return { ...state, ...payload }`(es7-snippets-semicolon)`

    default:
        return state`(es7-snippets-semicolon)`
    }
}
