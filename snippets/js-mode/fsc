# -*- mode: snippet -*-
# name: Creates a stateless React component with FlowTypes and ES6 module system
# key: fsc
# --
// @flow
import * as React from 'react'`(es7-snippets-semicolon)`
type Props = {
 $0
}`(es7-snippets-semicolon)`
export const ${`(es7-snippets-filename-base)`} = (props: Props) => {
 return (
  <div>

  </div>
 )`(es7-snippets-semicolon)`
}`(es7-snippets-semicolon)`
